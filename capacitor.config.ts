import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nouralhuda.app',
  appName: 'نور الهدى',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
